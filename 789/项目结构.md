novel2script/
├── frontend/ # 前端应用 (Vue 3 + TypeScript + Vite)
│ ├── public/ # 静态资源
│ ├── src/
│ │ ├── assets/ # 图片、字体等静态文件
│ │ ├── components/ # 可复用组件
│ │ │ ├── ChapterInput.vue # 章节文本输入与智能分章预览
│ │ │ ├── ScriptPreview.vue # 生成的 YAML 剧本预览（带语法高亮）
│ │ │ ├── CharacterEditor.vue # 角色表手动微调面板
│ │ │ └── ExportPanel.vue # 导出按钮（.yaml, .fountain）
│ │ ├── views/ # 页面视图
│ │ │ └── HomeView.vue # 主工作台页面
│ │ ├── services/ # 后端 API 调用封装
│ │ │ └── api.ts # axios 实例与接口定义
│ │ ├── types/ # TypeScript 类型定义（与 YAML Schema 对应）
│ │ │ └── script.ts
│ │ ├── router/ # Vue Router 路由配置
│ │ │ └── index.ts
│ │ ├── store/ # Pinia 状态管理（可选，用于跨组件共享数据）
│ │ │ └── script.ts
│ │ ├── App.vue # 根组件
│ │ └── main.ts # 入口文件，挂载 Vue 应用
│ ├── index.html
│ ├── package.json
│ ├── vite.config.ts
│ ├── tsconfig.json
│ └── .env.example # 环境变量（后端地址等）
│
├── backend/ # 后端服务 (Java 17 + Spring Boot 3)
│ ├── src/main/java/com/novel2script/
│ │ ├── Novel2ScriptApplication.java # Spring Boot 启动类
│ │ ├── config/ # 配置类
│ │ │ ├── WebConfig.java # CORS 跨域配置
│ │ │ └── LLMConfig.java # LLM 客户端配置（API Key、模型选择等）
│ │ ├── controller/ # REST 控制器
│ │ │ ├── ScriptController.java # 剧本转换接口 POST /api/v1/convert
│ │ │ └── HealthController.java # 健康检查接口
│ │ ├── service/ # 业务逻辑层（转换流水线）
│ │ │ ├── PreprocessorService.java # 章节识别、文本清洗、格式统一
│ │ │ ├── SceneSplitterService.java # 场景边界检测（混合策略）
│ │ │ ├── ElementExtractorService.java# 角色、对白、动作提取
│ │ │ ├── YamlAssemblerService.java # 将提取结果组装为结构化 YAML
│ │ │ └── LLMService.java # 封装 OpenAI / Claude 等 LLM 调用
│ │ ├── model/ # 领域模型（对应 YAML Schema）
│ │ │ ├── Script.java # 剧本顶层对象
│ │ │ ├── Meta.java # 元数据
│ │ │ ├── Character.java # 角色定义
│ │ │ ├── Scene.java # 场景
│ │ │ ├── SceneHeading.java # 场景标题（INT/EXT, 地点, 时间）
│ │ │ └── Beat.java # 节拍（action/dialogue/transition 等）
│ │ ├── dto/ # 数据传输对象
│ │ │ ├── ConversionRequest.java # 接收的请求体
│ │ │ └── ConversionResponse.java # 返回的响应体（含 YAML 字符串与警告）
│ │ ├── util/ # 工具类
│ │ │ ├── TextUtils.java # 文本分段、引号统一等
│ │ │ └── YamlValidator.java # YAML 格式校验与自动修复
│ │ └── prompt/ # 提示词模板（Java 常量或资源加载）
│ │ ├── SceneDetection.java
│ │ ├── DialogueExtraction.java
│ │ └── ActionRewrite.java
│ ├── src/main/resources/
│ │ ├── application.yml # 应用主配置（端口、LLM 参数等）
│ │ ├── prompts/ # 提示词文件（可选，方便调试修改）
│ │ │ ├── scene-detection.txt
│ │ │ ├── dialogue-extraction.txt
│ │ │ └── action-rewrite.txt
│ │ └── logback-spring.xml # 日志配置
│ ├── pom.xml # Maven 依赖（Spring Boot, Jackson, Lombok, OpenFeign 等）
│ ├── Dockerfile
│ └── .env.example
│
├── docs/ # 项目文档
│ ├── schema.md # 剧本 YAML Schema 定义文档
│ ├── api.md # API 接口文档
│ └── prompt-design.md # 提示词设计说明
│
├── docker-compose.yml # 一键启动前后端（含可选依赖）
├── .gitignore
├── README.md
└── LICENSE